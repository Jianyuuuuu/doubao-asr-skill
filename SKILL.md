---
name: doubao-asr
description: 使用豆包录音文件识别模型 2.0 标准版异步转写音频，导出实际返回的毫秒级词时间戳、逐句稿和句内/句间停顿候选；用于口播转写和剪辑前的节奏分析。
---

# 豆包录音文件转写

本技能负责转写和时间信息整理，不自动剪片。模型资源为 `volc.seedasr.auc`，请求模型名为 `bigmodel`。Python 3.10+ 标准库即可运行；音频提取可选用现有 FFmpeg。

## 输入与准备

- 获取音频本地副本、输出目录，以及用户授权提供给火山引擎读取的 HTTPS 音频链接。链接必须直接返回同一份音频；对象存储签名链接的有效期应覆盖排队和处理时间。
- 不擅自将本地文件公开或上传到额外服务。本脚本不含云存储上传器，也不依赖特定剪辑软件或审阅平台；若只有本地素材，先使用用户已有且授权的存储方案生成链接。
- 确认已开通标准版资源、使用者接受调用费用，并在本机配置凭据。已有转写授权即可继续，不新增重复确认。
- 凭据仅从进程环境变量或用户本机终端的隐藏输入取得。不要在聊天、提示词、脚本、`.env`、任务记录中索取/写入明文凭据；不要读取历史会话、浏览器凭据或系统钥匙串。

当前鉴权可选 `DOUBAO_API_KEY`；旧版应用选择 `--auth legacy`，使用 `DOUBAO_APP_ID` 和 `DOUBAO_ACCESS_TOKEN`。不需要 Secret Key。媒体地址使用 `DOUBAO_AUDIO_URL`；详细配置见 [README.md](README.md)。

## 执行

在本技能目录下运行（其他工作目录请用脚本的实际路径）：

```text
python scripts/doubao_asr.py transcribe --audio "audio.wav" --outdir "output/asr" --format wav
```

默认提交后查询一次；退出码 `2` 表示仍在处理，不是失败。再次运行同一命令会用保存的任务 ID 查询，不重复提交。可用 `--wait-seconds 60` 做有界等待；`--prompt` 仅用于用户本机真实交互终端。

- `enable_ddc=false`：保留口头语、重复等识别内容，方便后续决定怎么剪。
- `enable_itn=false`：避免数字等书面规范化对口播对照造成干扰。
- `show_utterances=true`：请求句和词时间信息。热词按需用 `--hotwords terms.txt`，UTF-8 每行一个，不硬编码某个人或节目的术语。
- 状态先保存再提交。网络中断后先查询原任务；提交不明确或服务端失败时，不自动另开收费任务。更换源音频须使用新输出目录。
- 标准版是异步接口，结果可能需要较长时间；不要把本地等待超时当作任务失败，也不要用高频轮询催任务。

## 输出与理解

输出目录包含 `task.json`、`response.json`、`transcript.json`、`words-ms.json`、`transcript.txt`、`gap-candidates.csv`。后五项含媒体内容，是私有工作产物，不应随技能发布。

**毫秒是单位，不是误差承诺。词时间戳不等于逐字强制对齐。**以实际返回的 `words` 为准；没有词时间戳时输出会标明句级或纯文本，不均分时间伪造字级结果。

停顿表通过相邻词时间范围的空档生成，区分同句/跨句。默认候选阈值 200 ms，可用 `--gap-ms` 调整。它不是静音检测，也不是自动删除清单；多人重叠、标点、漏词和分句误差会影响结果。剪辑前结合波形、原声和语义试听，保护首音、尾音、呼吸与必要停顿。需要字级精确切口时另做强制对齐/波形验证。

脚本时间基于所提交的音频起点。提取片段、剪切和变速后必须另存偏移/映射，不能直接套到新时间线。

已有响应可完全离线重新导出：

```text
python scripts/doubao_asr.py normalize --input "output/asr/response.json" --outdir "output/asr" --gap-ms 250
```

遇到鉴权、资源、媒体读取或任务错误，阅读 [references/api-notes.md](references/api-notes.md) 并核对官方文档。脚本只显示数字状态码，避免错误正文回显敏感输入。
